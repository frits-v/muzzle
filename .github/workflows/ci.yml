name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions: {}

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: "0"
  RUSTFLAGS: -Dwarnings

jobs:
  check:
    name: Check & Lint
    runs-on: ubuntu-latest
    permissions:
      contents: read # checkout
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false
      - name: Install Rust toolchain
        run: |
          set -euo pipefail
          [[ "${RUNNER_DEBUG:-}" == "1" ]] && set -x
          rustup toolchain install stable --profile minimal --component clippy,rustfmt
          rustup default stable
      - uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: cargo-check-${{ runner.os }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: cargo-check-${{ runner.os }}-
      - run: cargo fmt -- --check
      - run: cargo clippy --all-targets -- -D warnings
      - run: cargo check
      - name: Rustdoc warnings
        run: RUSTDOCFLAGS="-D warnings" cargo doc --no-deps
      - name: License exists
        run: test -f LICENSE
      - uses: jdx/mise-action@dba19683ed58901619b14f395a24841710cb4925 # v4.1.0
      - name: Shell lint (shellcheck + shfmt)
        run: |
          set -euo pipefail
          [[ "${RUNNER_DEBUG:-}" == "1" ]] && set -x
          mise exec -- shellcheck scripts/*.sh
          mise exec -- shfmt -d -i 2 -ci -bn scripts/*.sh

  test:
    name: Test
    runs-on: ubuntu-latest
    permissions:
      contents: read # checkout
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false
      - name: Install Rust toolchain
        run: |
          set -euo pipefail
          [[ "${RUNNER_DEBUG:-}" == "1" ]] && set -x
          rustup toolchain install stable --profile minimal
          rustup default stable
      - uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: cargo-test-${{ runner.os }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: cargo-test-${{ runner.os }}-
      - name: Unit tests
        run: cargo test
      - name: Integration tests
        run: cargo test --test integration
      - name: CLAUDE.md matches codebase
        run: cargo test --test claude_md

  build:
    name: Release Build
    runs-on: ubuntu-latest
    permissions:
      contents: read # checkout
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false
      - name: Install Rust toolchain
        run: |
          set -euo pipefail
          [[ "${RUNNER_DEBUG:-}" == "1" ]] && set -x
          rustup toolchain install stable --profile minimal
          rustup default stable
      - uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: cargo-build-${{ runner.os }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: cargo-build-${{ runner.os }}-
      - run: cargo build --release
      - name: Verify all 5 binaries
        run: |
          set -euo pipefail
          [[ "${RUNNER_DEBUG:-}" == "1" ]] && set -x
          for bin in session-start permissions changelog session-end ensure-worktree; do
            test -f "target/release/$bin" || { echo "MISSING: $bin"; exit 1; }
          done
          echo "All 5 binaries present"
      - name: Check binary sizes
        run: |
          set -euo pipefail
          [[ "${RUNNER_DEBUG:-}" == "1" ]] && set -x
          for bin in session-start permissions changelog session-end ensure-worktree; do
            size=$(stat -f%z "target/release/$bin" 2>/dev/null || stat -c%s "target/release/$bin")
            if (( size > 5242880 )); then
              echo "FAIL: $bin is ${size} bytes (> 5MB)"
              exit 1
            fi
            echo "OK: $bin (${size} bytes)"
          done

  workflow-lint:
    name: Workflow Lint
    runs-on: ubuntu-latest
    permissions:
      contents: read # checkout
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false
      - uses: jdx/mise-action@dba19683ed58901619b14f395a24841710cb4925 # v4.1.0
      - name: actionlint
        run: mise exec -- actionlint
      - name: zizmor (pedantic)
        run: mise exec -- zizmor --pedantic .github/workflows/
